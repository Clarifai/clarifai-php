language: php
# The version nightly now points to PHP 8.0.0-dev, which is not supported by PHPUnit just yet.
# Once it is, add it back there.
php:
  - '7.0'
  - '7.1'
  - '7.2'
  - '7.3'
  - '7.4snapshot'
  #- nightly

install: composer install
script: phpunit -c phpunit.xml -v
